# aliases: ls

```bash
@ls \
	cloud | local \
	.|<object-name> \
	[--delim <space>] \
	[--log <0>]
 . ls <object-name>.
@ls \
	cloud | local \
	objects \
	[--delim <space>] \
	[--log <0>] \
	[--prefix <prefix>]
 . ls objects.
@ls \
	<path>
 . ls <path>.
```
